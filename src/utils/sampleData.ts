import { Database } from './database';

export function initializeSampleData() {
  // No longer initializing any sample data
  // The system will start with an empty database
  console.log('System initialized with empty database');
}